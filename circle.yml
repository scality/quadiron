machine:
  pre:
    - pip install cpplint

compile:
  override:
    - cd src; make
    - cd test; make
    - cd tools; make

test:
  override:
    - ./lint.sh
    - cd test; ./tests
    - cd tools; ./test_ec.sh
